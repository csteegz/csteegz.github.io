source 'https://rubygems.org'

# Specify Ruby version for Netlify compatibility
ruby '3.1.0'

# GitHub Pages gem with version constraint for compatibility
gem 'github-pages', '~> 232', group: :jekyll_plugins

# Lock nokogiri to a version compatible with older Ruby if needed
# gem 'nokogiri', '~> 1.15.6'

# Additional Jekyll plugins (optional)
group :jekyll_plugins do
  gem 'jekyll-feed'
  gem 'jekyll-sitemap'
  gem 'jekyll-seo-tag'
end

# Required for Ruby 3.0+
gem 'webrick', '~> 1.8'
